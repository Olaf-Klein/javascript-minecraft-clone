{
  "name": "minecraft-clone-rust",
  "version": "0.1.0",
  "private": true,
  "description": "Convenience npm scripts for the Rust-based Minecraft clone",
  "license": "MIT",
  "scripts": {
    "dev": "cargo run",
    "start": "cargo run --release",
    "build": "cargo build --release",
    "check": "cargo check",
    "fmt": "cargo fmt",
    "lint": "cargo clippy"
  }
}